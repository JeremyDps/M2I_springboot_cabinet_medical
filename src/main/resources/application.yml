spring:
  datasource:
    url: jdbc:mysql://localhost:3355/cabinet_medical?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    root: WARN
    com.jd: INFO
  file:
    name: log.txt
    path: ./
